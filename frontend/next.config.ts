import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Build dang standalone de dong goi Docker gon nhe (chi copy .next/standalone).
  output: "standalone",
};

export default nextConfig;
